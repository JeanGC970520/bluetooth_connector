import 'package:flutter/material.dart';

class SearchDevicesView extends StatelessWidget {
   
  const SearchDevicesView({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('SearchDevicesView'),
      ),
    );
  }
}